<%= form_for([@product, @review]) do |f| %>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %><br>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>